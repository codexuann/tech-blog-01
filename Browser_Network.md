#### TCP, UDP
- tcp依靠三次握手建立可靠链接
	1. SYN : 客户端 -> 服务器 客户端进入SYN-SEND状态
	2. SYN-ACK: 服务端 -> 客户端 服务端进入SYN-RCVD状态
	3. ACK: 客户端 -> 服务端 双方进入ESTABLISHED状态
	- 为什么三次不是两次
		- 避免旧的请求建立链接
			- 网络拥堵使旧的syn包延迟到达, 服务端误认为是新连接延迟到达
		- 确认双方都有发送和接受的能力
			- 如果是两次, 服务端无法确认客户端能否收到自己的信息
		- 同步序列号
			- tcp需要依靠序列号保证传输, 三次握手可以保证双方的初始序列号都被对方确认接受
- tcp的四次挥手
	- 浏览器向服务端发送FIN, 进入fin_wait_1状态
	- 服务端收到后向浏览器发送ACK, 并开始向浏览器发送没传输完的数据, 浏览器进入fin_wait_2状态
	- 数据传输完毕, 服务端向浏览器发送FIN, 服务端进入last_ack状态
	- 浏览器向服务端发送ACK, 先进入time_wait状态, 等待2ms后, 为了确保服务端收到ACK, 双方进入closed状态
- tcp和udp的区别
	- tcp
		- 面向连接的, 在传输数据前后必须经过三次握手和四次挥手建立和释放连接
		- 保证数据可靠且按顺序
		- 报头大小至少20字字节, 效率较慢, 开销大
		- 面向字节流, 没有边界
		- 场景: web的http/https, 文件传输, 邮件传输
	- udp
		- 无连接的, 想发就发
		- 不保证可靠与按顺序, 可能发生丢包或乱序
		- 报头大小只有8字节, 效率高,  开销小
		- 面向数据报, 有边界
		- 场景: 在线游戏, 音视频通话, 直播, DNS查询
#### HTTP
- HTTP/1.1 vs HTTP/2
	- 二进制分帧
		- http/1.1报文是纯文本格式, 机器解析效率低, 容易出错
		- http/2将报文(herder和body)拆成一个个二进制帧, headers frame存放头部, data frame存放数据体
	- 多路复用
		- http/1.1: 队头堵塞问题
			- 如果一个请求(如大图片)处理很慢, 后面的请求就必须等着, 导致页面加载慢
			- 浏览器为了解决这个问题, 允许同一个域名同时建立最多6个tcp连接, 但这也消耗了更多的资源
		- http/2
			- 一个域名只用一个tcp链接
			- 通过Stream(流)的概念, 同时发送多个请求和响应, 并行传输
			- 因为二进制分帧, 数据拆散并标记StreamID, 帧可以乱序发送到了接收端再根据ID拼起来
			- 解决了http层面的队头堵塞问题, 提高了并发性能
	- 头部信息压缩
		- http/1.1 每次请求都要带上完整的header, 浪费带宽
		- http/2 引入`HPACK`算法
			- 静态字典: 常见的头部用一个数字代替
			- 动态字典: 之前发过的头部, 双方都记录下来, 下次只发差异数据
			- huffman编码: 对文本进行压缩
			- 显著减少了数据传输量, 提升速度
	- 服务端推送
		- http/1.1: 浏览器解析html发现需要css文件再去发请求, 中间有时间等待
		- http/2: 服务器可以发送html的同时主动发送css文件
- http/2 vs http/3
	- http2的缺陷
		- http/2本质还是基于tcp
			- 解决了http层面的队头堵塞问题, 如大文件加载慢, 但没有解决tcp层面的队头堵塞, 一旦tcp发生丢包, 整个tcp连接都要发生等待, 反而可能比有多个tcp连接的http/1.1更慢
			- 仍要通过三次握手建立连接, 耗时较高
		- 移动端切换wifi/蜂窝网络时, http需重新建立, 导致请求中断或超时
	- http3
		- 抛弃底层TCP协议, 改用基于UDP的QUIC协议, 兼顾udp的速度和tcp的可靠性
		- 彻底解决队头堵塞: 每个请求/响应是独立的Stream(流), 即使某个流包丢了, 其他流依然并行传输, 互不影响
		- 快速建联: 通过QUIC的加密握手, 代替传统的tcp三次握手和tls加密握手, 首次连接可快速完成
		- 移动端友好: QUIC使用`connection ID`识别连接. 而非ip地址, 连接不会断
- http vs https
	- 区别
		- http三次握手后传输数据, https三次握手后还需进行SSL/TLS握手
		- http传输所有数据都是明文, https加了一层SSL/TLS协议实现加密传输
	- 原理
		- 混合加密 - 防止窃听
			- 握手阶段, 通过非对称加密交换会话密钥
			- 数据传输阶段, 通过对称加密传输内容
		- 摘要算法 - 防止数据篡改
			- 使用摘要算法给数据生成指纹, 保证数据传输过程中没有被第三方篡改
		- 数字证书 - 身份认证
			- 数字证书由CA担保, 服务器需把公钥放到数字证书中发给客户端, 客户端通过验证数字证书合法性确认服务器身份
- http的请求方法
	- 常见的请求方法
		- GET-获取资源, POST-提交资源, DELETE-删除资源, PUT-替换资源(全量替换), PATCH-更新资源(如改昵称不改头像)
	- 设计API的考虑因素
		- 安全性
			- 请求是否会引起服务器状态的变化
			- GET是安全的, POST, DELETE, PUT, PATCH是不安全的
		- 幂等性
			- 多次执行相同的操作, 结果是否一致
			- GET, DELETE, PUT是幂等的, POST, PATCH(可以写如余额增加10元之类的逻辑)是不幂等的
	- GET和POST的区别
		- 语义
			- get是获取, 用于查询资源
			- post是提交, 用于创建或修改资源
		- 参数
			- get参数放在url的query string中, 会有长度限制
			- post参数放在请求体(body)中, 无长度限制
		- 安全
			- get参数暴露在url中, 不能用来传递密码或敏感信息
			- post参数在body中, 相对隐蔽, 但如果不使用https, 依旧是明文同样不安全
		- 缓存
			- get请求会被浏览器主动缓存, 可被收藏为书签
			- post请求不会被浏览器主动缓存, 不可收藏书签
- 状态码
	- 200 数据返回成功
	- 301 资源永久重定向(浏览器会缓存), 302 资源临时重定向, 304 协商缓存命中
	- 401 未认证 用户未登录, 403 未授权 用户权限不足, 404 找不到资源
	- 500 服务器代码错误, 502 网关错误, 503 服务器在维修, 504 网关超时
#### OSI七层模型
- 第七层 应用层: 应用程序产生数据
- 第六层 表示层: 将数据进行格式化, 压缩, 加密
- 第五层 会话层: 建立会话, 添加会话信息
- 第四层 传输层: 将数据分段, 并添加tcp/udp头部, 形成段
- 第三层 网络层: 将段加上IP头部(包含源/目的IP地址), 形成包
- 第二层 数据链路层: 将包加上帧头和帧尾(包含源/目的MAC地址, 校验信息), 形成帧
- 第一层 物理层: 将帧转化成比特流, 通过物理介质发送出去
#### 浏览器的五大核心进程
- 主进程
	- 负责浏览器外壳(地址栏, 书签等)
	- 管理子进程生死
	- 网络资源下载管理
- 渲染进程
	- 负责将htmlcssjs转换成用户能看到的页面, 每一个tab标签页都有一个独立的渲染进程, 一个页面崩了其他页面不受影响
	- 一个渲染进程中的五大核心线程
		- GUI线程
			- 负责画页面. 解析htmlcss, 构建dom树, 进行布局和绘制
			- js引擎线程在执行代码时, GUI线程会被冻结
		- JS引擎线程 (V8引擎)
			- 单线程, 解析和执行JS代码
		- 事件触发线程
			- 监听鼠标键盘等事件
			- 将事件的回调函数(计时器, 鼠标键盘事件, ajax异步请求的回调函数)放到任务队列中
		- 定时器触发线程
			- 处理setTimeout和setInterval的计时
		- 异步HTTP请求线程
			- 处理XHR请求
			- js发起一个请求, 该线程就去发网络包, 等请求返回后, 再把回调函数扔进任务队列通知js引擎
- GPU进程: 3D绘制和静态加速
- 网络进程: 加载网络资源
- 插件进程: 运行插件, 防止插件搞崩浏览器
#### 事件循环
- 为什么需要
	- js为了防止多线程同时操作dom导致乱套所以是单线程语言, 为了防止耗时请求(定时器, 网络请求)不阻塞主线程, 引入事件循环机制, 通过执行栈和任务队列的配合, 实现了非阻塞的异步编程
- 事件循环机制
	- 任务队列
		- 宏任务
			- script整体代码, setTimeout, setInterval, addEventListener
		- 微任务
			- promise, fetch
	- 执行流程
		- 执行栈执行同步代码
			- 这本身属于一个宏任务
		- 清空微任务队列
			- 当主线程执行栈为空时, 检查并执行微任务队列的所有微任务
			- 如果微任务又产生了微任务, 会继续执行, 直到微任务队列清空
		- 尝试渲染UI
			- 如果有必要 (到了屏幕刷新时间), 浏览器在这个时候UI渲染
			- requestAnimationFrame通常在这个时候执行
		- 执行下一个宏任务
			- 从宏任务队列中只取出一个宏任务, 执行. 然后重复事件循环
#### 浏览器垃圾回收机制(GC)
- 早期: 引用计数法
	- 看一个对象被引用的次数, 如果一个变量指向它, 引用+1, 如果不指向了, 引用-1, 当引用次数变为0时, 自动回收
	- 循环引用问题: 两个对象相互引用, 引用次数不可能为0, 无法回收
- 主流: 标记清除法 
	- 从根对象(window)出发, 顺着引用链找到的对象, 就标记为活动的. 找不到的对象就为垃圾直接清除
- v8引擎回收机制(以标记清除法为基础)
	- 栈垃圾回收: 存储执行上下文
		- 遵循后进先出, 函数执行结束后, 通过移动js指针清空上下文, 释放栈内存
	- 堆垃圾回收: 存储复杂对象
		- 新生代: 存活时间短的对象
			- 复制算法, 把内存一分为二, 只用一半, GC时, 把还活着的对象复制到另一半, 剩下的直接清空
		- 老生代: 存活时间长的对象
			- 标记清除: 标记活动的对象, 清空未标记的对象
			- 标记整理: 为了解决内存碎片问题, 将标记活动的对象往内存的一端移动, 腾出连续的大内存
#### 浏览器运行流程
- url输入后
	- 准备工作
		- url解析, 是否有非法字符
		- 检查缓存, 如果是强缓存且在有效期内, 直接获取本地资源不发送网络请求
		- DNS解析, 查找ip地址
			- 浏览器缓存, 系统缓存, 本地HOST文件, 路由器缓存, 运营商缓存, DNS服务器
			- DNS服务器查询是递归迭代的过程, 从根域名到顶级域名到二级域名三级域名直到找到目标IP
	- TCP三次握手
	- 发送http请求获取资源
		- 协商缓存的判断
	- 解析与渲染
		- 解析HTML代码, 构建DOM树
			- 遇到script代码会阻塞dom树的构建
		- 解析CSS代码, 构建CSSOM树
		- 将DOM树和CSS树合并, 生成渲染树
			- 渲染树只包含可见节点, display:none不会出现在渲染树中, visibility:hidden会
		- 布局/回流
			- 根据渲染树计算每个节点的位置, 大小等信息
		- 将页面分为多个图层
		- 绘制/重绘
			- 绘制每个节点的颜色, 背景, 边框等信息
		- 合成: 利用GPU加速, 将绘制好的多个图层叠加在一起最终显示在屏幕上
	- 四次挥手断开连接
- 浏览器每一帧都做了什么
	- 处理输入: 处理用户的点击, 滚动等
	- 执行js: 处理业务逻辑, settimeout等
	- requestAnimationFrame
		- 保证回调函数在浏览器下一次绘制之前执行, 避免丢帧
	- 样式计算: 计算每个元素的最终样式
	- 回流, 分层, 重绘, 合成, 显示

#### 浏览器存储
- http缓存体系
	- 强缓存
		- 浏览器通过检验Cache-Control:max-age, 如果未失效则不请求服务器, 返回200状态码, 读取缓存
	- 协商缓存
		- 浏览器请求服务器, 服务器比较Last-Modified或者Etag判断文件是否更新, 如果文件未发生更新, 返回304状态码, 浏览器读取缓存, 如果更新则返回200状态码和对应字眼
		- Last-Modified是记录保存的时间, 如果一秒内多次修改, 其不会变化, 如果文件没有修改只是重新保存, 其也会发生变化从而浪费带宽, 而E-tag可以精确表示文件是否变换
- Cookie、LocalStorage 和 SessionStorage
	- 都是本地存储, 都遵循同源策略
	- 与服务端交互
		- cookie: 每次发送http请求时, 自动携带在请求头中发给服务器
		- storage: 在浏览器本地, 不会发给服务器
	- 储存大小限制
		- cookie: 4kb左右
		- storage: 5mb甚至更多
	- 生命周期
		- cookie: 由开发者决定, 如果设置了max-age, 就在过期时间前有效, 如果没有设置, 就是会话级有效, 关闭浏览器就失效
		- localstorage: 永久有效, 除非手动清除
		- sessionstorage: 会话级有效, 只要关闭当前标签页或浏览器数据就会被清除
	- 应用场景
		- cookie: 用于用户鉴权, 存储token或者session ID
		- localstorage: 存储不需要频繁变动的持久化数据, 如用户偏好设置
		- sessionstorage: 存储临时敏感数据与单次流程数据, 如表单
- IndexDB
	- 浏览器内置数据库
	- 与localstorage的区别
		- 存储容量: 比ls大的多
		- 存储类型: ls只能存字符串, indexDB可存任意类型如js对象, 二进制blob, 文件
		- 操作方式: ls是同步的, indexDB是promise异步的, 操作不会阻塞主线程
	- 应用场景
		- 离线应用: 在线文档, 在线编辑器
		- 大文件存储: 用户上传的图片, 视频草稿
		- 复杂查询: ls需要读取到内存中遍历查询, indexDB可以建立索引查找, 速度快
- Service Worker
	- 运行在浏览器后台的独立线程, 完全异步(promise), 必须是https
	- 拦截网络请求: 拦截客户端发送的所有请求, 自由决定去网络请求数据, 还是直接返回缓存数据, 例如网络中断, 就从缓存中读取数据并返回, 网络通畅, 就请求服务器并更新缓存. 实现资源缓存和离线访问
	- 消息推送: 接受服务器发出的消息, 并弹窗告知用户, 即使页面关闭, 也能在后台运行
	- 后台同步: 在网络恢复时, 自动把离线期间生产的请求(如表单提交)发送除去